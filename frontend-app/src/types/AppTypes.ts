/**
 * Свойства вкладок меню первого уровня.
 * @param label Надпись.
 * @param key Ключ.
 * @param link Ссылка.
 */
export interface TabProps {
    label: string;
    key: string;
    link: string;
}